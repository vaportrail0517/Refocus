package com.example.refocus.core.model

enum class OverlaySuggestionMode {
    Goal,   // 「やりたいこと」モード
    Rest    // 「休憩」モード
}

enum class OverlayColorMode {
    SingleColor,
    Threshold,
    Gradient
}